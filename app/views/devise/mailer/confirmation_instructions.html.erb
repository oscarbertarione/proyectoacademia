<p><%= t('.greeting', recipient: @resource.email, default: "Bienvenido #{@resource.email}!") %></p>

<p><%= t('.instruction', default: 'Puedes confirmar tu cuenta haciendo click en el siguiente enlace:') %></p>

<p><%= link_to t('.action', default: 'Confirmar mi cuenta'),
         confirmation_url(@resource, confirmation_token: @token, locale: I18n.locale) %></p>
